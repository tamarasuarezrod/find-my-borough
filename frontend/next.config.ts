/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      '127.0.0.1',
      'localhost',
      'api.findmyborough.uk',
      'res.cloudinary.com',
    ],
  },
}

module.exports = nextConfig
